{
  "name": "@obcda/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Pure domain logic: label normalization, canonical keys, version handling. No runtime/DB/LLM dependencies.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
