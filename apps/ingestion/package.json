{
  "name": "@obcda/ingestion",
  "version": "0.1.0",
  "private": true,
  "description": "Official-source ingestion: SourceAdapter contract, fetch guards, normalization pipeline (詳細設計仕様書 §4). First concrete adapter: BsddRestAdapter (#13).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dry-run": "esbuild src/cli.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/cli.mjs --log-level=warning && node dist/cli.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@obcda/domain": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
